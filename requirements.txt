# Runtime deps for Streamlit Cloud deployment.
# Only what streamlit_app.py imports (reads precomputed CSV/JSON outputs).
# For full pipeline retraining locally, see requirements-dev.txt.
pandas==2.2.2
numpy==1.26.4
plotly==5.22.0
streamlit==1.39.0
